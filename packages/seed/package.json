{
  "name": "seed",
  "version": "1.0.0",
  "main": "./lib/index.js",
  "typings": "./lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc -b .",
    "preseed": "yarn build && firebase use test && firebase functions:delete updateBookList",
    "seed": "node .",
    "postseed": "firebase deploy --only functions:updateBookList"
  },
  "dependencies": {
    "firebase": "^6.0.2",
    "firebase-admin": "^7.3.0"
  },
  "devDependencies": {
    "@types/node": "^11.13.9",
    "typescript": "^3.4.5"
  }
}
